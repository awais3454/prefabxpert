import * as THREE from 'three';
import { ProcessorContext } from './SharedLogic';

/**
 * Handles Step 6: Bekleding (Cladding).
 */
export function processStep6Bekleding(obj: THREE.Object3D, context: ProcessorContext) {
  return false;
}
