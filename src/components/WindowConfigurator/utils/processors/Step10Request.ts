import * as THREE from 'three';
import { ProcessorContext } from './SharedLogic';

/**
 * Handles Step 10: Request (Aanvraag).
 */
export function processStep10Request(obj: THREE.Object3D, context: ProcessorContext) {
  return false;
}
