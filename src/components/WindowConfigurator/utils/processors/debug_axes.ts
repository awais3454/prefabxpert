// import * as THREE from 'three';
// export function debugAxes(scene: THREE.Object3D) {
//   scene.traverse((obj) => {
//     if ((obj as THREE.Mesh).isMesh) {
//       const box = new THREE.Box3().setFromObject(obj);
//       const size = new THREE.Vector3();
//       box.getSize(size);
//       // Debug: Object info logging
//     }
//   });
// }
