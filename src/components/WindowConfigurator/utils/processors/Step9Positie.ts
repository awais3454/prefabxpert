import * as THREE from 'three';
import { ProcessorContext } from './SharedLogic';

/**
 * Handles Step 9: Positie.
 */
export function processStep9Positie(obj: THREE.Object3D, context: ProcessorContext) {
  return false;
}
